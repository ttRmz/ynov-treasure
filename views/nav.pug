block nav
    nav.navbar
        a.navbar-brand(href="")
            img(src="./images/brand.png", alt="y")
        ul.navbar-links
            li 
                a.btn.btn-silver.text-uppercase(href="/account") #{user.username}
            li 
                a.btn.btn-silver(href="/logout") Logout